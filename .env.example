# ============ Supabase ============
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...anon-public-key
SUPABASE_SERVICE_ROLE_KEY=eyJ...service-role-key

# ============ Mayar (payment gateway) ============
# API key dari dashboard.mayar.id → Settings → API Keys
MAYAR_API_KEY=xxxxx
# Webhook token dari Settings → Webhook (untuk verify callback authenticity)
MAYAR_WEBHOOK_TOKEN=xxxxx
# false = sandbox (api.mayar.club), true = production (api.mayar.id)
MAYAR_IS_PRODUCTION=false

# ============ Google Service Account ============
# Buat service account di Google Cloud Console, enable Drive + Sheets API,
# download JSON key, paste email & private_key di bawah.
# private key: jaga newline-nya — paste apa adanya pakai \n literal, runtime akan unescape.
GOOGLE_SERVICE_ACCOUNT_EMAIL=finance-bot@your-project.iam.gserviceaccount.com
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIE...\n-----END PRIVATE KEY-----\n"

# ============ Resend (email) ============
# Optional — kalau kosong, email aktivasi cuma di-log ke console (tidak gagalkan flow)
RESEND_API_KEY=re_xxxx
RESEND_FROM_EMAIL=Finance Bot <noreply@your-domain.com>

# ============ App ============
APP_URL=http://localhost:3000

# ============ Deploy (VPS) ============
# Domain untuk Traefik routing (tanpa http/https)
APP_DOMAIN=catetin.co
# Nama Docker network tempat Traefik jalan (biasanya n8n_default)
TRAEFIK_NETWORK=n8n_default

# ============ WAHA (WhatsApp HTTP API) ============
# Untuk kirim activation message ke user setelah bayar & best-effort
# pre-warming LID mapping. Kalau kosong, langkah ini di-skip (non-blocking).
WAHA_URL=http://72.61.208.96:3001
WAHA_SESSION=default
WAHA_API_KEY=your-waha-api-key

# ============ Bypass mode ============
# true  = /api/checkout langsung aktivasi tanpa Mayar (untuk testing)
# false = wajib selesai bayar di Mayar, aktivasi terjadi di webhook callback
BYPASS_PAYMENT=true
